## Supabase Rules — READ BEFORE ANY DATABASE ACTION

1. **Panteray project = `znepjevqtbhijqvlxpmq`** — this is the ONLY project for panteray/platform
2. **DO NOT use MCP Supabase tools** (`mcp__8b9afab5-*`) for panteray/platform — they only access casdex which is a DIFFERENT project
3. **DO NOT touch casdex** (`uhapfjjanaieucfripuo`) for any panteray work
4. **Database migrations** — write SQL migration files in the repo, ask the user to run them
5. **DO NOT add devices** to the device library without explicit permission from Dexter
